class BookingService {
    static createBooking(bookingDetails) {
      // Logique pour appeler l'API pour créer une réservation d'hébergement
    }
  
    static fetchBookings(userId) {
      // Logique pour appeler l'API pour récupérer les réservations d'un utilisateur spécifique
    }
  
    static cancelBooking(bookingId) {
      // Logique pour appeler l'API pour annuler une réservation spécifique
    }
  }
  
  export default BookingService;
  