class ApiService {
    static fetchListings() {
      // Logique pour appeler l'API pour récupérer les annonces
    }
  
    static fetchListingDetails(listingId) {
      // Logique pour appeler l'API pour récupérer les détails d'une annonce spécifique
    }
  
    static searchListings(searchParams) {
      // Logique pour appeler l'API de recherche d'annonces avec les paramètres spécifiés
    }
  }
  
  export default ApiService;
  