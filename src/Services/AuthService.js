class AuthService {
    static login(username, password) {
      // Logique pour appeler l'API d'authentification pour connecter l'utilisateur
    }
  
    static logout() {
      // Logique pour appeler l'API de déconnexion de l'utilisateur
    }
  
    static register(userDetails) {
      // Logique pour appeler l'API d'inscription pour créer un nouvel utilisateur
    }
  }
  
  export default AuthService;
  