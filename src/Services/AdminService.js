class AdminService {
    static fetchUsers() {
      // Logique pour appeler l'API pour récupérer la liste des utilisateurs (fonctionnalité d'administration)
    }
  
    static fetchUserDetails(userId) {
      // Logique pour appeler l'API pour récupérer les détails d'un utilisateur spécifique (fonctionnalité d'administration)
    }
  
    static deleteListing(listingId) {
      // Logique pour appeler l'API pour supprimer une annonce spécifique (fonctionnalité d'administration)
    }
  }
  
  export default AdminService;
  